# ============================================================
# ⚠️  WARNING: DO NOT PUT REAL SECRETS IN THIS FILE!
# ============================================================
# This is a TEMPLATE file. Copy to .env.prod and fill in values.
# NEVER commit .env.prod to version control!
# ============================================================

# PostgreSQL Database
POSTGRES_DB=<CHANGE_ME>
POSTGRES_USER=<CHANGE_ME>
# Use Docker secrets for password: POSTGRES_PASSWORD_FILE

# Security Tokens (Generate new ones!)
JWT_SECRET=<CHANGE_ME>
MONITORING_TOKEN=<CHANGE_ME>

# Domain Configuration
DOMAIN=<CHANGE_ME>
API_DOMAIN=<CHANGE_ME>
CORS_ORIGINS=<CHANGE_ME>
FRONTEND_URL=<CHANGE_ME>
EMAIL_FROM=<CHANGE_ME>

# Database Connection (password should use Docker secrets)
DATABASE_URL=postgres://<USER>:<PASSWORD>@postgres:5432/<DB>?sslmode=disable
