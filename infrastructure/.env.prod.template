# ============================================================
# NAS.AI Production Environment Configuration
# ============================================================
# WICHTIG: Diese Datei nach .env.prod kopieren und ausfüllen!
# Kommando: cp .env.prod.template .env.prod
# ============================================================

# ============================================================
# DATABASE SECRETS (PostgreSQL)
# ============================================================
# Datenbank-Name (Standard: nas_db)
POSTGRES_DB=nas_db

# Datenbank-Benutzer (Standard: nas_user)
POSTGRES_USER=nas_user

# Datenbank-Passwort (MUSS geändert werden!)
# Generieren mit: openssl rand -base64 32
POSTGRES_PASSWORD=CHANGE_ME_STRONG_PASSWORD_HERE

# ============================================================
# SECURITY SECRETS
# ============================================================
# JWT Secret für Token-Signierung (MUSS geändert werden!)
# Generieren mit: openssl rand -base64 64
JWT_SECRET=CHANGE_ME_JWT_SECRET_HERE

# Monitoring Token für Metriken-Endpunkte (MUSS geändert werden!)
# Generieren mit: openssl rand -base64 32
MONITORING_TOKEN=CHANGE_ME_MONITORING_TOKEN_HERE

# ============================================================
# DOMAIN CONFIGURATION
# ============================================================
# Haupt-Domain (ohne https://)
DOMAIN=example.com

# API Sub-Domain (ohne https://)
API_DOMAIN=api.example.com

# CORS Origins (kommasepariert, MIT https://)
CORS_ORIGINS=https://example.com,https://api.example.com

# Frontend URL (MIT https://)
FRONTEND_URL=https://example.com

# ============================================================
# EMAIL CONFIGURATION
# ============================================================
# Absender-Email für System-Emails
EMAIL_FROM=noreply@example.com

# Optional: SMTP-Server für Email-Versand
# SMTP_HOST=smtp.example.com
# SMTP_PORT=587
# SMTP_USER=noreply@example.com
# SMTP_PASSWORD=smtp_password_here

# ============================================================
# OPTIONAL: Advanced Configuration
# ============================================================
# Log Level (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Rate Limiting (Requests pro Minute)
RATE_LIMIT_PER_MIN=100

# Backup-Konfiguration
BACKUP_SCHEDULE="0 2 * * *"  # Täglich um 2 Uhr morgens
BACKUP_RETENTION_COUNT=7      # Behalte 7 Backups

# ============================================================
# AI Model Configuration
# ============================================================
AI_MODEL_TYPE=sentence-transformers
AI_MODEL_NAME=sentence-transformers/all-MiniLM-L6-v2
MODEL_LOAD_RETRIES=3
INFERENCE_TIMEOUT=30

# ============================================================
# AI SERVICE CONFIGURATION
# ============================================================
# AI Knowledge Agent URL (internal Docker network)
# Default: http://ai-knowledge-agent:5000 (container name)
AI_SERVICE_URL=http://ai-knowledge-agent:5000

# Ollama LLM URL (running on host machine)
# Default: http://host.docker.internal:11434
OLLAMA_URL=http://host.docker.internal:11434

# LLM Model for RAG queries
LLM_MODEL=qwen2.5:3b

# ============================================================
# ICLOUD PHOTO BACKUP
# ============================================================
# Apple ID für iCloud Photo Sync
ICLOUD_APPLE_ID=your_email@icloud.com
